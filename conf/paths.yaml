paths:
  # Use PROJECT_ROOT if set, otherwise fall back to "."
  project_root: ${oc.env:PROJECT_ROOT,.}

  datasets:
    mmf: ${paths.project_root}/data/MMF

  output_root: ${paths.project_root}/results
